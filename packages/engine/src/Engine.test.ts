import { NeuledgeEngine } from './Engine.js';

describe('Engine', () => {
  describe('NeuledgeEngine', () => {
    it('should constract', () => {
      const engine = new NeuledgeEngine();

      expect(engine);
    });
  });
});
