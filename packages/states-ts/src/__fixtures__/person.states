"The old person state"
@deprecated(reason: "please use version 2")
state Person_old {
    "Person ID"
    @id id: Number = 1

    @deprecated(reason: "not safe format")
    name: String = 2
}

"""
The latest Person state!
Got it??
"""
state Person from Person_old {
    -name

    firstName: String = 1
    lastName?: String = 2
}